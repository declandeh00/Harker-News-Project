/**
 * @file Runs the app and styles it
 * @author Declan de Haas
 */

//Adjusted the globals.css to make it look better.
import '@/styles/globals.css';

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}
